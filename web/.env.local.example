# Get this free at https://dashboard.privy.io — create an app, enable Google
# login + embedded wallets, add Arc Testnet (chain 5042002) as a custom chain,
# then paste the App ID here. Without it, Google login stays hidden and the app
# still works with MetaMask / OKX / Rabby.
NEXT_PUBLIC_PRIVY_APP_ID=
